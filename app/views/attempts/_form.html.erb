<%= form_with url: '/attempts/add_student', method: :post do |form| %>
  <p><%= form.label :student_id, "Выберите студента" %></p>
  <p><%= form.select :student_id, @fios%>
  <div class='col-auto'>
    <%= submit_tag("Сохранить", class: "btn btn-info", id: 'btn') %>
</div> 
<% end %>
<%= form_with url: "/attempts/add_discipline", method: :post do |form| %>
    <%= submit_tag("Дальше", class: "btn btn-danger", id: 'btn') %>
<% end %>  

<div class="modal-footer">
<button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Закрыть</button>
</div>