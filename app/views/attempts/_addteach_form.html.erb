<%= form_with url: '/attempts/add_tea', method: :post do |form| %>
  <p><%= form.label :teacher_id, "Выберите преподавателя, принимавшего экзамен" %></p>
  <p><%= form.select :teacher_id, @teachers%>
    <div class='col-auto'>
    <%= submit_tag("Сохранить", class: "btn btn-info", id: 'btn') %>
  </div> 
<% end %>
<%= form_with url: "/attempts/add_other", method: :post do |form| %>
    <%= submit_tag("Дальше", class: "btn btn-danger", id: 'btn') %>
<% end %>   
</div>
<div class="modal-footer">
<button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Закрыть</button>
</div>