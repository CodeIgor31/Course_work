<%= form_with url: '/attempts/add_dis', method: :post do |form| %>
  <p><%= form.label :discipline_id, "Выберите дисциплину" %></p>
  <p><%= form.select :discipline_id, Discipline.all.map {|el| [el.name, el.id]}%>
   <div class='col-auto'>
    <%= submit_tag("Сохранить", class: "btn btn-info", id: 'btn') %>
</div> 
<% end %>
<%= form_with url: "/attempts/add_variant", method: :post do |form| %>
    <%= submit_tag("Дальше", class: "btn btn-danger", id: 'btn') %>
<% end %>   
</div>
<div class="modal-footer">
<button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Закрыть</button>
</div>